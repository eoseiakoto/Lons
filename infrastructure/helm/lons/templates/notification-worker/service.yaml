apiVersion: v1
kind: Service
metadata:
  name: {{ include "lons.fullname" . }}-notification-worker
  labels:
    {{- include "lons.componentLabels" (dict "context" . "component" "notification-worker") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.notificationWorker.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lons.selectorLabels" (dict "context" . "component" "notification-worker") | nindent 4 }}
