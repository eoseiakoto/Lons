apiVersion: v1
kind: Service
metadata:
  name: {{ include "lons.fullname" . }}-scoring-service
  labels:
    {{- include "lons.componentLabels" (dict "context" . "component" "scoring-service") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.scoringService.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lons.selectorLabels" (dict "context" . "component" "scoring-service") | nindent 4 }}
