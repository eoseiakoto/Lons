{{- if .Values.restServer.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "lons.fullname" . }}-rest-server
  labels:
    {{- include "lons.componentLabels" (dict "context" . "component" "rest-server") | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "lons.fullname" . }}-rest-server
  minReplicas: {{ .Values.restServer.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.restServer.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.restServer.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
