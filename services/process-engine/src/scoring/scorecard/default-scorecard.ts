// `DEFAULT_SCORECARD` was moved to `@lons/shared-types` so the database
// seed can reference it without depending on this package. Re-exported
// here to preserve the public `@lons/process-engine` API.
export { DEFAULT_SCORECARD } from '@lons/shared-types';
